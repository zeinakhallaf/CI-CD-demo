name: CI/CD step
on:
 push: 
  branches: [ "main" ]
jobs:
  run-script :
   runs-on: ubuntu-latest
   steps:
   - name: checkout code
     uses: actions/checkout@v3
   - name: Run my shell script
     run: bash script.sh
